/**
 * Onboarding page. Phase 1 scaffold: placeholder status.
 * Phase 2/3 add: one-time model download with progress + verification.
 */
const statusEl = document.getElementById('onboarding-status');
statusEl.textContent = 'Scaffold build — model setup arrives in Phase 2.';
